import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dxocr.app',
  appName: 'DX OCR',
  webDir: 'www',
  server: {
    androidScheme: "http"
  },
};

export default config;
